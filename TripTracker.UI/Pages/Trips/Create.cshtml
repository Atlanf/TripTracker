@page
@model TripTracker.UI.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Trip</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">


            <div asp-validation-summary="ModelOnly" danger=""></div>

            @* <div asp-validation-summary="ModelOnly" class="text-danger"></div> // without tag-helpers *@
            
            @* @await  Component.InvokeAsync("EditTrip", Model.Trip) *@
            
            <vc:edit-trip trip="Model.Trip"></vc:edit-trip>

            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
